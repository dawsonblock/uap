interface:
  display_name: "Uap"
  short_description: "Repo-specific patterns and workflows for uap"
  default_prompt: "Use the uap repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true